"use strict";
exports = {};
let message;
let student = { name: 'John', age: 25 };
let nestedGuy = { nestedObj: { name: 'Joe', age: 30 } };
// Works with more or less anything.
// Issue is, we can probably only have one 'type' assigned to one variable.
